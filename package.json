{
  "name": "bukkapay-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npx react-scripts start",
    "build": "cd frontend && npx react-scripts build",
    "build:dev": "cd frontend && npx react-scripts build",
    "start": "cd frontend && npx react-scripts start"
  }
}
