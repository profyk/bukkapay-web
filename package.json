{
  "name": "bukkapay-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend && bun install && bun run start",
    "build": "cd frontend && bun install && bun run build",
    "build:dev": "cd frontend && bun install && bun run build",
    "start": "cd frontend && bun install && bun run start"
  }
}
